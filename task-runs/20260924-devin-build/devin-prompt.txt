GOAL      Implement three bounded modules in repo github.com/agent-next/art-harness (branch off main): benchmark/, pipeline/, connectors+skills. Faithful to the cited parameters below; every parameter traceable.
CRITERIA
1. `make check` (ruff + pytest) passes on your branch in a fresh clone after `pip install -e ".[dev]"`.
2. benchmark/: 
   - benchmark/task_statement.md: verbatim task statement, quote exactly: "characterize the RT and predict its function: report every feature you judge important, the evidence for it, how confident you are, and what experiment would test your main prediction." plus context sentences: loci drawn from a large sequence collection because each encodes a member of one related group of RTs; nothing else is known. Each attempt ends with a report and a structured submission of findings: each finding = {claim, evidence, confidence 0-1}.
   - benchmark/rubric.py: data model of 10 binary claims grouped 3 x 5-prime-noncoding-tract, 2 x RT, 5 x partner (paper Fig 4B; exact claim wordings unknown -> use GAP: placeholders with these counts). Grading rule from paper: claim counted ONLY when submission asserts it as a conclusion, not as one of several hedged possibilities; no credit for over-claims specified in rubric. Judge sees ONLY submission + rubric; a programmatic keyword screen preselects relevant findings and does not contribute to grade.
   - benchmark/levels.py: build L1..L5 environments: L1 prompt contains 2 RT protein sequences in context, no tools; L2 = the two corresponding loci as text in prompt, no tools; L3 = no sequence in prompt, all 96 loci as files in offline env with gene calling, HMMER, BLAST+, MMseqs2, MAFFT, FastTree, ViennaRNA, Python; L4 adds predicted structure for every ORF, Foldseek with local reference structure DB, PyMOL, US-align; L5 adds literature search, web access, software installation. Inputs described: 96 ART loci (1,093 to 26,554 bp) with anonymized identifiers, gene calls, Pfam matches, 1,131 predicted protein structures. Input fixtures may be synthetic for tests.
   - benchmark/run_benchmark.py: CLI orchestrating attempts = models x levels x N attempts (default 100, paper had 3,500 total), each attempt an isolated run with max output budget 1,000,000 tokens, producing report + structured submission; grading per Methods: judge model scores 10 claims, score 0-10, "recognized repeat array" = repeat-array claim asserted. Offline session backend interface: accept a pluggable backend callable; default backend raises unless env ARTHARNESS_ALLOW_LIVE=1 (reuse src/artharness/runner/base.py ClaudeCodeBackend if convenient).
   - tests: rubric assertion-vs-hedge rule, level builders, one end-to-end offline attempt with a stub backend.
3. pipeline/: parameter-exact wrappers, each step a .sh or .py with `# paper:` citation comments. --dry-run mode prints the underlying commands. Content:
   - pipeline/census/01_hmmsearch.sh: 52 Pfam/myRT/Toro profile HMMs, hmmsearch --noali -Z 8 --domZ 8 -E 0.01 --domE 0.01 (paper: 3,391,714 candidates)
   - pipeline/census/02_filter.py: retain complete RT core profile coverage >= 0.75, class-specific min length 225-346; discard fragments, weak hits (bitscore < 25, coverage < 0.35, no YxDD), other; expected 203,381 retained
   - pipeline/census/03_cluster.sh: MMseqs2 easy-cluster --min-seq-id 0.5 -c 0.8 --cov-mode 0 -> 198,290 clusters
   - pipeline/census/04_classify.py: 3 tiers: 38 lineage HMMs with class-specific bitscore floor + >=10 bit margin; MMseqs2 vs labeled RTs bitscore >= 150, cov >= 0.5, id >= 30%; FastTree -lg -gamma with 5-nearest-labeled-leaves agreement. Expected classes: retron 11,517 / UG 10,759 / group II intron 5,461 / DGR 3,857 / CRISPR-assoc 2,067 / group II-like 827 / Abi 680 / novel 25,737 / unplaced 137,385.
   - pipeline/census/05_sample_neighborhoods.py: anchors: Abi 680 + group II-like 827 exhaustive; DGR 1,000 and CRISPR 1,000 random; UG 700 proportional to clade size; retron 1,000 random + 1 control; group II intron 500; novel 60/clade x 29 clades; unplaced 440 largest lineages + 200 random singletons -> total 7,308 anchors; up to 3 loci per anchor from each of Logan/ENA/JGI/NCBI, 10 kb flanks.
   - pipeline/census/06_score_partners.py: family assignment by best Pfam-A match or MMseqs2 cluster; three promotion filters: (1) conserved within >=1 RT clade (>=10% of loci or >=3 loci); (2) independent occurrences >=3 clusters at 90% id, >=3 biosamples, beside RTs of >=2 classes; (3) permutation test P <= 0.05 for RT proximity + same strand or within 100 bp of adjacent gene. Controls designated beforehand.
   - pipeline/art_family/family_definition.sh: BLASTP from MarsHill RT GenBank QQM14740.1; profile HMM from 12 GenBank phage RTs (10 Staphylococcus + 2 Listeria phage LPJP1), MAFFT --auto + hmmbuild; hmmsearch HMMER 3.4 E <= 1e-5 -> 9,100 proteins; keep 350-900 aa (n=4,379); pool with 12 phage RTs + 2,019 myRT reference RTs; DIAMOND blastp --very-sensitive E <= 1e-5, link at bitscore >= 140; connected component containing MarsHill RT = 117 proteins, no reference RT; expand clusters, add members >= 400 aa (706 further) -> 823 full-length RTs; MMseqs2 easy-cluster --min-seq-id 0.9 -c 0.8 -> 230 clusters; FAMSA align + FastTree LG; ART clade = smallest clade containing every locus with type I partner + >=500 nt non-coding upstream; 95 representatives ART_01..ART_95; geNomad 1.12.0 database release 1.9 end-to-end default for contig classification.
   - pipeline/art_family/phylogeny.sh: 774-sequence set (98 ART+NART, 617 retron, 59 other); RT domain = Pfam RVT_1 envelope +/- 40 residues; MAFFT L-INS-i; trimAl -gappyout -> 254 columns; IQ-TREE 3.1.2 ModelFinder BIC -> Q.pfam+F+R6, 1,000 ultrafast bootstrap + 1,000 SH-aLRT, midpoint rooted.
   - pipeline/rnaseq/sa1_infection.sh: BioProject PRJNA836150 (SA1 infection of Staphylococcus lentus, 12 libraries, 3 time points); fastp 1.3.6 (adapter trimming off, poly-G trimming, min length 12); Bowtie2 --very-sensitive -X 1000 --no-unal vs SA1 genome MW218148.1 + S. lentus chromosome NZ_CP059679.1; keep MAPQ >= 10; TPM over 259 features.
   - pipeline/db/build_subset.sh: prodigal-gv 2.10.0 default params; filters: exclude incomplete CDS, non-standard amino acids, > 8,000 residues, degenerate k-mer repeats, >= 50% low-complexity via tantan; DIAMOND linear-time clustering at 90% then 70% identity with >= 80% coverage of shorter, then cascaded 50% identity >= 80% mutual coverage; representative = member closest to 80th percentile of length.
   - tests: a flag-fidelity test asserting required flags appear in scripts (grep-based oracle), plus unit tests for any Python logic.
4. connectors + skills:
   - src/artharness/connectors/literature.py: Europe PMC REST client (search + full-text URL resolution), offline-first: live flag, recorded fixture test mode.
   - src/artharness/connectors/protein_db.py: sqlite schema (clusters, proteins, contigs, coordinates, biosamples, pfam) + MMseqs2 wrapper interface (search against representatives or 365M reference set UniProt+nr at 90%).
   - src/artharness/connectors/gpu_queue.py: job queue interface for structure prediction (ESMFold esmfold_v1 or ColabFold 1.6.1 alphafold2_ptm, 5 models 3 recycles; A100/L4), dispatch stub, no real GPU calls.
   - skills/survey/*.md (7 guides) and skills/tools/*.md (5 guides) named per REPRODUCTION.md section 4; each guide = one page of method (counts of independent occurrences, operonic arrangement scoring vs window background, adjudicating candidates, testing novelty claims, annotating uncharacterized families, proposing mechanisms with counterfactual evidence, drawing gene-neighborhood figures; tools: database use, literature access, GPU dispatch, report editing, bioinformatic tools).
   - tests for literature client with fixture, protein_db schema roundtrip.
5. Every file you create carries the parameter provenance comments (`# paper Methods p.NN` where known; use `GAP:` where the paper does not state a value). Do NOT invent numbers silently.
6. Commit small, Conventional Commits, NO AI attribution lines, on branch devin/build-modules. Open ONE PR to main at the end with title "feat: benchmark, pipeline wrappers, connectors, skills (reproduction modules)".
SCOPE     Only benchmark/, pipeline/, src/artharness/connectors/, skills/, tests/. Do NOT modify src/artharness core (records/orchestrator/roles/tournament/arrays/config), README, AGENTS.md, or workflow files. Do not merge. Do not touch other repos.
INPUTS    github.com/agent-next/art-harness default branch main. Python >= 3.11. `pip install -e ".[dev]"` then `make check`. Paper context: all needed parameters are in this prompt; do not fetch external sources.
VERIFY-BY In your clone: `make check` exits 0; `python -m pytest -q` all pass; `bash pipeline/census/01_hmmsearch.sh --dry-run` prints the exact hmmsearch command; `python -m benchmark.run_benchmark --help` works. Paste real command output.
OUTPUT    Receipt: files changed, test output, dry-run outputs, PR URL. End with GOAL_COMPLETE, GOAL_BLOCKED, or GOAL_NEEDS_INPUT.

You ARE Devin. Use run_subagent with profiles explore/builder/tester/verifier for parallel disjoint work. Never profile subagent_explore. Do not exec ccz/grok/cx/devin.
