GOAL      S3 independent gate (execution-based): review the S2 fix round of this repo (agent-next/agent-discovery) and return a verdict.
CRITERIA  Writer != reviewer: these 9 commits were written by the session agent + finders; you are the independent vote. Every finding: file:line + concrete failure scenario (inputs/state -> wrong behavior) + confidence 0-1 + severity critical|major|minor. Citations must be exact or the finding is rejected. FIRST read REVIEW.md at repo root (always-check rules 1-9, skip lists, bars) and docs/paper-notes.md (constants ledger). You MUST verify by execution, not prose: run `make check` yourself (expect ruff clean + 163 passed) and reproduce each blocking finding with a real command before confirming it. Findings you could not reproduce go to checked_does_not_hold with the command you tried.
SCOPE     Commits ef779ed..5bd846e on branch fix/s2-review-round (HEAD must equal 5bd846e — verify with git rev-parse HEAD first; run `git log --oneline ef779ed^..HEAD`). Focus: (1) src/artharness orchestrator/records/roles/runner/tournament/accounting changes; (2) src/artharness/arrays.py chain/tolerant-window/median/PWM changes — check the new skip rule against the paper wording 'single skipped copy allowed' and the true-median change against 30% tolerance; (3) benchmark levels enforcement (disallowed_for/materialize), rubric hedging (_HEDGES list, _is_hedged), run_benchmark opt-in + resume dedupe; (4) connectors literature flags/errCode/pagination + protein_db min_seq_id removal; (5) pipeline rnaseq counting model (fastp min-30, samtools -f 2 -e tlen, featureCounts -p -s 2 -t CDS), census tier3 wiring + UG cap, build_subset tantan keying + dedup, forensics plural/wrap; (6) tests/test_s2_review_fixes.py — do the power tests actually have power? In particular check: test_chains_reject_a_second_skipped_copy expects a legal 4-copy chain with ONE skip; test_soundness_auto_lose_is_load_bearing arithmetic; the revision-completion-check stall path (EmptyRevision semantics) against orchestrator._dispatch.
INPUTS    This checkout (LOCAL branch fix/s2-review-round; not pushed anywhere — the working tree IS the ref, pinned by HEAD SHA).
VERIFY-BY `make check` (paste the tail: expect 163 passed) + per-finding repro commands (paste real output).
OUTPUT    findings[] {file,line,scenario,confidence,severity} · checked_does_not_hold[] · VERDICT: SHIP | FIX-FIRST. End with GATE_COMPLETE.
